#include <stdio.h>
main(){ /*program illustrating integer truncation*/
   printf("answer1 is the integer %d", 9%4); /*1*/
   printf("\nanswer2 is the integer %d", 17%3); /*2*/
}

// the % operator is the modulo operator and returns the remainder of a division
// the remainder part is always an integer
// the program returns the remainder parts of the two divisions