#include <stdio.h>

void main(void){
   // suppose there are 10 lists to be averaged
   
   for(int list_no = 1; list_no <= 10 ; list_no++){
      // suppose there are 10 values in each list
      for(int value_no = 1; value_no <= 10 ; value_no++){
         // code to calculate sum and average
      }
   }
}